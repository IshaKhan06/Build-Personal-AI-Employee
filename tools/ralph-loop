#!/bin/bash
# Ralph Wiggum Loop Runner (Gold Tier)
# Usage: ./ralph-loop "Process multi-step task in Needs_Action" --max-iterations 20

# Change to the project directory
cd "$(dirname "$0")/.."

# Run the Python script with the provided arguments
python tools/ralph_loop_runner.py "$@"
